torch>=2.1.0
numpy>=1.24.0
tiktoken>=0.5.0
datasets>=2.14.0
tqdm>=4.65.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0
transformers>=4.40.0
huggingface_hub>=0.22.0
pillow>=10.0.0
pywinauto>=0.6.9
comtypes>=1.4.0
# Optional — chỉ cần khi dùng LoRA fine-tune (python cli.py finetune --lora)
peft>=0.10.0
# Optional — giao diện chat Gradio (python app.py) cho Colab / link công khai
gradio>=5.0.0

# Agent framework & tools
# (tất cả thư viện agent dùng stdlib Python, không cần cài thêm)
# MCP client: httpx đã có ở trên
